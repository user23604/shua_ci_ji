# 刷词机最终发布报告

版本：`2026-07-26-final-stable-v3`

## 发布判断

本版在 `final-audited-v2` 基础上完成第二次独立复审和修复。已关闭上一轮发现的恢复锁、回滚校验、历史阻塞错误、备份选择、本地 JSON 损坏静默降级和活动草稿持久化顺序问题。保持一个 Gist、一个 `sync.json`、手机和平板顺序切换的既有方式，不引入设备分片或新的同步 schema。

## 关键修复

- 回滚失败后设置硬恢复锁；启动、同步和“当前数据非空”均不能自动解除。
- 恢复锁只允许由事务前检查点/明确备份的业务哈希验证成功，或独立救援流程明确确认后解除。
- 权威业务 JSON 无法解析时保存原始文本到隔离键并停止云同步，防止空数据或混合状态上传。
- 新鲜且完整的 PATCH 写后验证可清除历史认证/合并 blocker，不再误报“本地在验证期间变化”。
- 备份选择使用词典序优先级，事务恢复优先匹配 `pre_overwrite` 检查点。
- 活动草稿先持久化 dirty/业务修订，再清 pending，降低浏览器强杀造成未上传统计的窗口。
- 诊断包增加恢复锁来源、时间和检查点哈希；救援导出保留损坏原始数据。

## 保持不变

- 一个用户共用一个 Gist 和一个 `sync.json`；
- 学习动作先写本地，云端失败不阻塞刷词；
- 匿名 GET → JSONP → 必要时认证 GET；写入使用认证 PATCH；
- 远端变化且本地有数据时确定性合并；
- PATCH 前重读、写后验证，结果未知先回读确认；
- 手机和平板按顺序使用。活动时长/次数继续用幂等 `max` 合并；用户已确认不会同时学习，因此不为并发统计引入复杂迁移。

## 自动验证

执行：

```bash
npm run build
npm test
```

结果：全部通过。生产 bundle 由 44 个源码模块生成，大小 415,379 字节；可维护源码共 10,243 行，最大核心模块 462 行。额外通过 JavaScript 语法、顶层函数重复、JSON、版本一致性、PAT 模式、Service Worker 资源和 ZIP 解压后复测。

完整问题、修改证据、测试矩阵和外部边界见 `FINAL_AUDIT_REPORT.md`。

## 外部边界

纯前端不能保证任意中国大陆网络或 VPN 节点都可访问 GitHub。只要 VPN/代理真实接管当前浏览器到 `api.github.com` 的 GET、OPTIONS、PATCH，以及必要时到 `gist.githubusercontent.com` 的请求，当前代码路径具备完整读写能力。真实大陆手机/VPN链路仍需部署后真机验收；本版保证外部网络失败不会演变为本地数据覆盖、无限重试、假成功或无法刷词。
