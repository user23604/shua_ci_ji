# 刷词机最终发布报告

版本：`2026-08-31-round-archive-v1`

## 发布判断

本版基于 `shua_ci_ji-7_27_1` 增量修改，可直接作为下一版部署目录。核心新增是“学习轮次归档”：用户主动封存当前一整轮学习记录，然后在不丢历史、不改原始词库、不丢同步配置的前提下开启一轮干净的新刷词。

## 新增行为

- “归档复盘”顶部新增“归档当前轮”，确认前必须填写归档名称，可填写多行备注。
- 归档保存每本词书的已删/重难点语义标记、普通/重难点进度、学习活动、Unit 完成记录以及归档时设置。
- 归档成功后，当前轮的标记、进度、活动统计、持久进度游标、活动草稿和本机刷词会话全部清空；CSV 原词库、词书/播放设置、Gist 配置、备份和历史归档不删除。
- 新增“历史归档”页签，只读展示归档名称、时间、备注、总数以及各词书摘要。历史快照内部保留真实单词 ID 和进度，后续若增加恢复/调用能力无需重新猜测旧轮数据。

## 多设备与同步安全

- 每一轮带 `generation + roundId`。同一轮继续使用原来的 LWW 标记、最远进度、活动/Unit 统计合并；不同轮绝不逐字段混合，generation 更高的整套当前学习状态获胜。
- 因此，设备 A 归档并进入新一轮后，设备 B 的旧进度/旧标记不会再被“更远进度”规则复活；设备 B 更新后同步也会清掉自己的旧刷词会话。
- 历史归档按 archive id 做确定性并集，不随当前轮重置删除。
- 新云端 envelope 使用 `version: 2`；本版可读取旧 v1。仍停留在 7/27 旧代码的客户端无法识别 v2，会按原安全门禁拒绝覆盖，而不是把新字段静默剥掉后写回。
- 业务哈希 schema 升到 4，round/archive 纳入 hash、备份判空、Gist payload、rescue 和写后校验。

## 失败保护

- 归档前刷新当前持久游标/活动草稿并创建覆盖前安全备份。
- 归档 + 新轮次 + 当前轮清空在本地按事务执行；任一存储写入或写后校验失败都会恢复归档前状态。
- 写入前估算新同步 envelope；超过现有 900 KiB Gist 可靠阈值时停止归档，避免出现“本地归档成功但云端从此无法可靠同步”。

## 验证

执行 `npm run build` 和 `npm test` 全部通过。新增 `tests/round_archive_regression_test.js`，覆盖名称/多行备注、快照保留、当前轮彻底重置、archive-only 数据判空、跨设备旧数据防复活、v2 envelope、新轮远端应用清理旧会话以及模拟存储失败自动回滚。原同步、网络兜底、静态质量、UX v4、Audio v5、rescue 和三份 CSV 完整性测试均继续通过。
