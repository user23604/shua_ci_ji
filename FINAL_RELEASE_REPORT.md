# 刷词机最终发布报告

版本：`2026-07-27-final-ux-sync-v4`

## 发布判断

本版可直接替换 v3 部署目录。保留上一版全部同步安全修复，并完成恢复优先启动、归档强制折叠、两级长按选择、整 Unit 重刷、手动中文和非线性滑动。

## 关键行为

- 已登录启动：读取本地学习会话，恢复书目、队列、当前词和中文显示状态，直接进入刷词卡片并暂停。无有效会话时按当前设置构建默认队列，同样暂停进入。
- 归档：全部 Unit 初始 `open=false`，CSS 明确隐藏未打开 Unit 的 `.word-list`。Unit 长按和单词长按互斥，滚动会取消长按。
- 组合刷词：只取当前归档标签中、属于所选 Unit 的单词，按词库原顺序组成复盘队列。
- 批量撤销：所选词写入 `markStates.value=null`，保留 tombstone 与逻辑时间，确保多设备合并能够传播撤销。
- 手动中文：关闭自动 reveal timer；卡片中部或右侧点击优先显示中文。暂停态只显示中文并保持暂停，第二次点击才恢复；播放态直接进入中文阶段。
- 滑动：原始位移经过非线性增益再驱动卡片和判定；点击抑制仍基于原始位移，降低误触。
- 同步：自动同步关闭时，初始化、heartbeat、视图检查、联网恢复、学习上传和统一调度均不访问云端；手动按钮继续走读取、合并、PATCH、回读校验。

## 验证

执行 `npm run build` 和 `npm test`，46 个源码模块构建成功，全部发布测试通过。新增 `tests/ux_v4_regression_test.js`。

当前容器 Chromium 受组织策略限制，任何本地 HTTP/file URL 均返回 `ERR_BLOCKED_BY_ADMINISTRATOR`，因此未把容器浏览器截图测试冒充为真机验证。代码级状态机、静态结构、bundle 一致性、同步与数据完整性测试均已执行；发布后仍应按真机清单在平板完成一次长按、滑动和恢复验证。
