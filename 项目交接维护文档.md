# 刷词机项目交接维护文档

> 这份文档给后续接手开发者使用。目标不是重新说明产品创意，而是把当前已经跑通的功能契约、代码入口和高风险改动点写清楚，避免后续加功能时把已有逻辑改反或改坏。

## 1. 当前状态与接手原则

当前项目是纯前端单页应用，无构建流程，直接由 `index.html` 加载 `style.css`、`app.js` 和词库 CSV，可部署到 GitHub Pages。

维护时以当前代码和本文档为准。`旧版文档归档/刷词机项目提示词.md` 是初始需求，不是最终实现合同；项目已经多轮迭代，例如现在支持横屏和竖屏、云同步已改为自动推拉、朗读倍速与读前/中文/读后绝对时间已拆开控制。

所有 Markdown 都是本地内部资料，不应提交到 GitHub。当前 `.gitignore` 使用 `*.md` 忽略 Markdown；如果某个 `.md` 已经被 Git 跟踪，需要在本地执行 `git rm --cached -- "*.md" "旧版文档归档/*.md"` 取消跟踪，但不要删除本地文件。

接手开发时请遵守这几条：

- 不要为了小功能引入构建工具、框架或大型 UI 库。
- 不要把 CSV 解析器改成 `split(",")` 这类脆弱实现。
- 不要让刷词页一次渲染大量单词；卡片区只应保留当前卡片和下一张卡片。
- 不要用 `top/left/margin` 做卡片滑动动画，继续使用 `transform: translate3d(...)`。
- 改 `app.js`、`style.css` 或核心资源后，同步升级 `sw.js` 的 `CACHE_NAME`。

## 2. 文件地图

主要文件：

- `index.html`：应用入口，只提供挂载节点和资源引用。
- `style.css`：全部 UI、响应式、卡片热区、滑动反馈、入场动画和深色模式。
- `app.js`：全部业务逻辑，包括鉴权、设置、词库解析、刷词、语音、统计、复盘、Gist 同步。
- `manifest.json`：PWA 配置。
- `sw.js`：Service Worker，核心文件 Network-First，词库和图标 Stale-While-Revalidate。
- `项目交接维护文档.md`：本地内部交接文档，已被 `.gitignore` 忽略，交接时优先给新开发者看。
- `旧版文档归档/旧_README_INTERNAL.md`：旧内部 README，已归档，只作历史参考。
- `旧版文档归档/刷词机项目提示词.md`：初始需求提示词，已归档，只作历史参考。

词库文件：

- `27ky_shanguo_gaopin.csv`
- `hongbaoshu_bikao.csv`
- `hongbaoshu_jichu.csv`

闪过词典频段规则：

- Unit 1-12：高频词
- Unit 13-21：中频词
- Unit 22-30：低频词

这些标签会显示在设置页、刷词页、阶段总结和归档分组里。不要把第一本词书整体再命名为“高频词”，否则 13 以后会造成误导。

原始资料目录，只保留源数据和备份词表，不再放主说明文档：

- `项目原数据词表以及交代md/`

## 3. 本地运行与检查

不要用 `file://` 打开，词库 `fetch()` 和 Service Worker 在这种方式下不可靠。用本地 HTTP 服务：

```powershell
python -m http.server 4173 --bind 127.0.0.1
```

访问：

```text
http://127.0.0.1:4173/
```

基础检查：

```powershell
node --check app.js
node --check sw.js
```

如果改了 `manifest.json`：

```powershell
python -m json.tool manifest.json
```

## 4. 最重要的交互契约

### 4.1 左右滑动和左右点击不能改反

这是当前用户确认过的正确逻辑，后续不要凭直觉改：

| 用户动作 | 业务结果 | 旧卡退出动画 | 新卡入场动画 | 核心代码 |
| --- | --- | --- | --- | --- |
| 左滑 `left` | 下一个词 | 向左飞出 | 自动播放无入场；手动下一个从右轻入 | `triggerCardDirection()` -> `advanceWord("manual")` |
| 右滑 `right` | 上一个词 | 向右飞出 | 从左轻入 | `triggerCardDirection()` -> `goPrevious()` |
| 点击左侧 `tap-left` | 上一个词 | 向右飞出 | 从左轻入 | `renderCardSwipeControls()` / `triggerCardDirection()` |
| 点击右侧 `tap-right` | 下一个词 | 向左飞出 | 从右轻入 | `renderCardSwipeControls()` / `triggerCardDirection()` |
| 上滑/点上侧 `up` | 标记已斩并进入下一个 | 向上飞出 | 无特殊入场 | `markCurrent("known")` |
| 下滑/点下侧 `down` | 只标记重难点，不跳词，不暂停 | 不飞出，只做轻量反馈 | 当前卡继续播放时序 | `markUnknownInPlace()` |
| 点击右侧提示条 `↑/↓/←/→` | `↑` 已斩，`↓` 生词，`←` 上一个，`→` 下一个 | 复用同一套动画/反馈 | 复用同一套入场规则 | `bindGesturePanelControls()` -> `triggerCardDirection()` |

特别注意：

- `tap-left` 和 `tap-right` 是点击热区名称，不是滑动方向。
- 左滑是“向左滑动”，代表下一个；点击左侧是“点左边区域”，代表上一个。
- 点击左侧的动画应该向右翻；点击右侧的动画应该向左翻。
- 第一张卡片右滑或点击左侧时不能循环到最后一张，只能回弹。
- 右侧提示区不是纯说明，四个条形区域都可以点击。它们必须继续复用 `triggerCardDirection()`，不要另写一套方向逻辑。
- 右侧提示区的 `←/→` 是按钮含义，不是卡片滑动方向：`←` 必须是上一个，`→` 必须是下一个。
- 连续快速点击上一词/下一词必须按点击次数执行。动画中再次点击会进入 `state.navQueue`，不要改回“动画中直接忽略”。

### 4.2 暂停和恢复

当前正确行为：

- 正常播放时，点击卡片中间内容区：暂停。
- 暂停后，再点击当前卡片任意非撤销按钮区域：恢复播放。
- 暂停后不显示单独的“恢复播放”按钮。
- 暂停态点击左右/上下热区也只恢复播放，不触发切词或标记。
- 撤销按钮、设置按钮、统计按钮、归档按钮等真实按钮不能触发卡片暂停。
- 恢复播放时会有轻量反馈：卡片短暂描边脉冲，并显示短暂的“继续播放”小提示；不要加全屏遮罩或影响阅读的浮层。

相关代码：

- `pausePlaybackFromCard()`
- `resumePlayback()`
- `bindCardGesture()`
- `state.playbackPaused`
- `state.resumeFeedback`
- `.word-card--resume-feedback`
- `.resume-feedback`

### 4.3 自动播放、朗读倍速和绝对时间

设置页文案为“朗读倍速”，范围是 `0.5x` 到 `10.0x`，滑杆步进 `0.05x`。

朗读倍速只影响：

- 英文朗读 `SpeechSynthesisUtterance.rate`
- 中文简读 `SpeechSynthesisUtterance.rate`

这些时间都是绝对毫秒值，不能再除以倍速或做开平方缩放：

- 读前停留：`preReadDelay`，默认 `500ms`，范围 `0-3000ms`。
- 中文出现延迟：`zhDelay`，默认 `1200ms`，范围 `0-5000ms`。
- 读后停留：`retentionPause`，默认 `850ms`，范围 `0-5000ms`。
- 无语音时的静音阅读预算：由文本长度估算，不受朗读倍速缩放。

每张卡片的时序：

1. T0 立即显示英文卡面，中文保持隐藏。
2. T0 同时启动中文出现延迟计时；`zhDelay = 0` 时立即显示中文。
3. 先等待读前停留。
4. 执行英文朗读；如果英文朗读关闭或不可用，走英文估算阅读时间。
5. 英文阶段结束后，等待中文已经显示，再执行中文朗读；如果中文朗读关闭，仍给中文估算阅读时间。
6. 中文阶段结束后等待读后停留。
7. 自动模式下进入下一词；手动模式下停在当前词，直到用户手势/点击切换。

相关常量：

- `PLAYBACK_RATE_MIN`
- `PLAYBACK_RATE_MAX`
- `PLAYBACK_RATE_STEP`
- `SPEECH_RATE_MAX`
- `PRE_READ_DELAY_DEFAULT`
- `PRE_READ_DELAY_MIN`
- `PRE_READ_DELAY_MAX`
- `RETENTION_PAUSE_MIN`
- `RETENTION_PAUSE_MAX`
- `RETENTION_PAUSE_DEFAULT`

相关函数：

- `playbackRate()`
- `speechRate()`
- `zhRevealDelayMs()`
- `preReadDelayMs()`
- `quietBudgetMs()`
- `phaseGapMs()`
- `retentionPauseSettingMs()`
- `scheduleWordTimers()`

### 4.4 中文延迟为 0 的要求

`zhDelay = 0` 时必须立即显示中文，不等待英文朗读结束。

当前实现把 `revealZhAfterDelay(token)` 作为独立任务启动：

```js
const revealTask = revealZhAfterDelay(token);
```

后续不要把中文显示重新放回“英文朗读结束之后才开始计时”的路径，否则会复发“设置 0 仍然有延迟”的问题。

### 4.5 朗读不能被截断

Web Speech API 在不同浏览器、系统语音包、安卓 WebView 中表现不一致。当前策略是：

- 朗读开始前允许有短启动兜底。
- 朗读真正开始后，不再用硬超时强制取消。
- 通过 `speechSynthesis.speaking/pending` 轮询和 `onend/onerror` 判断结束。
- 如果启动失败，才用静音阅读预算兜底。

不要重新增加“朗读开始后按估算时长取消”的逻辑，这会导致读音读一半被截断。

相关函数：

- `speakWithHighlight()`
- `waitForSpeechVoices()`
- `setSpeechPhase()`
- `clearSpeechPhase()`
- `simulateZhHighlight()`

### 4.6 已斩词过滤与撤销

当前规则：

- 上滑或点击右侧 `↑` 会把当前词加入 `marks.known`，表示已斩。
- 下滑或点击右侧 `↓` 会把当前词加入 `marks.unknown`，表示重难点，但不进入下一个词。
- 已斩词在下一次进入主学习队列时会被过滤，不再继续出现和播放。
- 本次正在刷的内存队列不会立刻删除刚上滑的词，所以用户可以右滑回去及时撤销。
- 撤销按钮必须写明来源：已斩显示“撤销上滑”，重难点显示“撤销下滑”。
- 当前卡如果已经有标记，无论是手动回退还是自动播放再次走到该词，都必须显示对应撤销按钮。
- 复盘和归档仍然可以显示已斩词，因为那是用户主动查看历史数据。

相关代码：

- `buildStudyUnitWords()`
- `buildUnknownStudyWords()`
- `markCurrent()`
- `markUnknownInPlace()`
- `undoMark()`
- `undoLabelForMark()`
- `goPrevious()`

### 4.7 重难点词库刷词模式

设置页“重难点词库”打开后，开始刷词不再使用主词库未斩队列，而是使用当前词书 `marks.unknown` 里的词。

范围规则：

- 选择具体 Unit：只刷该 Unit 的重难点词。
- 选择“整本词书”：刷当前词书全部重难点词。
- 重难点模式复用所有声音、朗读倍速、读前/中文/读后停留、手动模式、手势、统计和总结逻辑。
- 重难点为空时必须给明确提示，不能白屏。

进度键：

- 主词库进度：`progress:<bookId>`。
- Unit 重难点进度：`unknown_progress:<bookId>:unit:<unit>`。
- 整本重难点进度：`unknown_progress:<bookId>:book`。

重难点模式的恢复进度不能写入主词库 `progress:<bookId>`。云同步 payload 里有 `unknownProgress` 字段，读取时必须先做结构清洗。

相关代码：

- `currentUnknownScope()`
- `unknownWordsForScope()`
- `buildUnknownStudyWords()`
- `loadUnknownProgress()`
- `saveUnknownProgress()`
- `collectUnknownProgressForBook()`
- `applyUnknownProgressPayload()`

### 4.8 手动模式

设置页和刷词页侧栏都有“手动模式”控制。打开后，当前卡完整走完读前停留、英文阶段、中文阶段和读后停留，但读后停留结束不自动进入下一词。

手动模式下仍然允许这些动作切词：

- 左滑或点击卡片右侧：下一个。
- 右滑或点击卡片左侧：上一个。
- 点击右侧提示栏 `←/→`。
- 上滑或点击 `↑` 标记已斩并进入下一个。

下滑或点击 `↓` 仍然只标记重难点，不切词。

### 4.9 英文单词视觉

主卡片英文单词当前字重是 `font-weight: 520`，比之前的超粗字重更轻。后续调样式时要保持单词单行和 `fitActiveWord()` 缩放能力，不要为了变细破坏长词适配。

## 5. 核心代码入口

### 5.1 设置和词书

- `BOOKS`：三本词书配置。
- `SHANGUO_BOOK_ID` / `unitBandLabel()` / `unitDisplayLabel()` / `bookContextLabel()`：闪过词典 Unit 高频/中频/低频标签。
- `DEFAULT_SETTINGS`：默认设置。
- `normalizeSettings()`：设置清洗和范围归一化。
- `rememberCurrentBookSettings()` / `restoreBookSettings()`：按词书记住 Unit、队列模式、重难点范围、朗读倍速、读前停留、中文延迟、读后停留、手动模式、语音和显示模式。
- `renderSetup()`：设置页 UI。
- `bindSetupEvents()`：设置页事件绑定。

新增设置项时要同步处理：

- 默认值 `DEFAULT_SETTINGS`
- 清洗逻辑 `normalizeSettings()`
- 每本书记忆 `PER_BOOK_SETTING_KEYS`
- 设置页渲染和绑定
- Gist 同步 payload，如果该设置需要跨设备同步

当前需要按词书记忆的节奏/显示设置包括：`queueMode`、`unknownScope`、`rate`、`preReadDelay`、`zhDelay`、`retentionPause`、`manualMode`、`summaryMode`、`summaryCount`、`speakEn`、`speakZh`、`highOnly`。

### 5.2 词库解析

词库加载和解析集中在 `loadBooks()`、`parseCsv()`、`mapWords()` 一带。

当前 CSV 解析器支持双引号、字段内逗号、字段内换行和双引号转义。新增词库时优先转换为根目录 CSV，并在 `BOOKS` 和 `sw.js` 静态资源列表里同步添加。

### 5.3 刷词页渲染

- `renderFlashcard()`：刷词页主渲染，保存进度、渲染当前卡和下一卡、绑定事件、启动自动播放。
- `renderWordCard()`：单张卡片 HTML。
- `renderCardSwipeControls()`：边缘点击热区和方向反馈层。
- `bindGesturePanelControls()`：右侧四个长条提示按钮的点击绑定。
- `fitActiveWord()`：长英文单词单行缩放。
- `processNavigationQueueSoon()` / `queueNavigationAction()`：上一词/下一词连续点击队列。

卡片 DOM 策略：

- 当前卡：`#activeCard`
- 下一卡：`#nextCard`
- 不要把整组单词全部渲染到 DOM。

### 5.4 手势和点击

- `bindCardGesture()`：绑定点击、拖动、滑动。
- `finishPointer()`：判断滑动阈值和轻拨速度。
- `swipeDirectionFromDelta()`：把位移转换为 `left/right/up/down`。
- `updateCardSwipeFeedback()` / `setCardSwipeFeedback()`：边缘低强度光影提示。
- `triggerCardDirection()`：最终动作分发，最容易被改反，改前先看本文 4.1。
- `cardActionFromDirection()`：把卡片滑动方向、点击热区名和右侧提示栏按钮统一映射为业务动作。
- `snapBack()`：未触发动作时回弹。
- `animateOut()`：旧卡飞出动画。

`style.css` 里的热区约定：

- `.card-tap-zone--left` / `.card-tap-zone--right`：左右完整高度，各占 28% 宽度。
- `.card-tap-zone--up` / `.card-tap-zone--down`：中间上/下区域，避开左右热区。
- `.word-card--enter-from-left` / `.word-card--enter-from-right`：快速切词时的轻量入场动画。
- `.word-card--resume-feedback` / `.resume-feedback`：暂停后恢复播放的轻量反馈。
- `.gesture-item`：右侧提示区的整条可点击按钮，箭头和文字只是视觉内容，不是唯一点击点。
- `prefers-reduced-motion: reduce` 下入场动画会关闭。

### 5.5 进度、标记和统计

- `advanceWord(reason)`：进入下一个词，更新扫过/已斩/重难点计数。
- `goPrevious()`：回上一个词，显示中文并在需要时展示撤销标记按钮。
- `markCurrent(kind)`：标记已斩或重难点，同一词只属于一个标记集合。
- `undoMark(wordId)`：撤销当前词标记。
- `commitCurrentCardActivity()`：写入学习时长和接触词。
- `renderBreak()` / `continueAfterBreak()`：阶段总结和继续学习。

计数规则：

- 手动下一个和自动下一个都算扫过。
- 上滑/下滑同时算扫过和对应标记。
- 回退上一个不应额外增加扫过。
- 临时复盘不应污染主学习进度。

### 5.6 统计、复盘、归档

- 统计数据来自 `activity:<bookId>`。
- 已斩和重难点来自 `marks:<bookId>`。
- 今日/本周/本月复盘基于 `activity.days[date].wordIds`，不是基于已斩或重难点列表。
- 本轮重难点复盘是临时队列，结束后返回原位置。

### 5.7 Gist 同步

本项目是个人设备同步方案，不是多人账号系统。

本地键：

- `vocab_machine_cloud_v1`：PAT 和 Gist ID。
- `vocab_machine_sync_meta_v1`：本地最近更新时间。
- `vocab_machine_settings_v1`：设置。
- `progress:<bookId>`：进度。
- `marks:<bookId>`：标记。
- `activity:<bookId>`：学习活动。

同步文件：

- Gist 里的 `sync.json`

冲突策略：

- 优先比较整体学习进度深度。
- 进度相同才比较 `updatedAt/localUpdatedAt`。
- 空 Gist、缺 `sync.json`、`{}` 或缺关键字段时，视为未初始化并静默推送本地完整结构。
- 损坏 JSON 或不兼容版本不要自动覆盖。

## 6. PWA 和缓存

当前 `sw.js` 使用：

- 核心文件 Network-First：`index.html`、`app.js`、`style.css`、`manifest.json`、`sw.js`
- 静态资源 Stale-While-Revalidate：CSV 和 `icon.svg`

改动核心文件后必须：

1. 升级 `CACHE_NAME`，当前版本是 `vocab-machine-v23`。
2. 保持 `CACHE_PREFIX = "vocab-machine-"` 不变。
3. 本地 HTTP 跑一遍基础流程。
4. 部署后如果看到旧版本，先检查浏览器 Service Worker 是否已激活新版本。

## 7. 新增功能建议流程

### 7.1 新增 UI 设置

1. 在 `DEFAULT_SETTINGS` 增加默认值。
2. 在 `normalizeSettings()` 做范围清洗。
3. 如果按词书保存，加入 `PER_BOOK_SETTING_KEYS`。
4. 在 `renderSetup()` 渲染控件。
5. 在 `bindSetupEvents()` 绑定变更。
6. 确认 Gist 同步是否需要带上新字段。
7. 跑设置页切换词书的回归检查。

### 7.2 新增接口或同步字段

1. 不要把新的远端字段直接写回本地，先做结构清洗。
2. 保持空数据、缺字段、损坏 JSON 都不会白屏。
3. 需要跨设备同步的字段加入完整 payload。
4. 更新冲突策略说明，避免新字段被旧设备覆盖。

### 7.3 新增词书

1. 根目录放新 CSV，文件名尽量 ASCII。
2. 在 `BOOKS` 增加词书项。
3. 确认 `mapWords()` 能识别字段。
4. 在 `sw.js` 的静态资源里加入新 CSV。
5. 升级 `CACHE_NAME`。
6. 验证词数、Unit 数、设置页切换和刷词页渲染。

## 8. 回归测试清单

每次改动刷词页、语音、设置、同步或 PWA 后，至少检查这些：

- `node --check app.js`
- `node --check sw.js`
- 密码 `ky2027` 能进入设置页。
- 设置页能切换三本词书，并恢复每本词书自己的 Unit、队列模式、重难点范围、朗读倍速、读前停留、中文延迟、读后停留、手动模式、语音和释义设置。
- 闪过词典 Unit 1-12 显示高频词，13-21 显示中频词，22-30 显示低频词。
- 朗读倍速滑杆范围是 `0.5x` 到 `10.0x`，只影响 Web Speech 语速。
- 读前停留、中文出现延迟、读后停留滑杆可调，并按词书记忆；高倍速下这些时间仍保持绝对毫秒值。
- 暂停后点击当前卡片继续时，有轻量“继续播放”反馈，不使用遮罩，不影响阅读。
- 英文主词字重保持较细，当前为 `font-weight: 520`。
- 中文延迟设为 `0` 时，中文立即显示；非 0 时新卡不能先闪出中文再隐藏。
- 英文朗读不会被硬超时截断。
- 自动模式会继续跳下一个词，不需要手动点击。
- 手动模式下读后停留结束不自动跳词，只能靠手势/点击切换。
- 左滑进入下一个。
- 右滑返回上一个。
- 点击左侧返回上一个，旧卡向右飞出。
- 点击右侧进入下一个，旧卡向左飞出。
- 点击右侧提示区四个长条按钮分别能触发已斩、重难点、上一个、下一个。
- 连续快速点击下一词三次，实际前进三词；连续快速点击上一词同理。
- 第一张卡片右滑或点左侧只回弹，不跳到最后。
- 点击中间暂停，暂停后再点当前卡片恢复。
- 暂停后点击左右热区只恢复，不切词。
- 上滑/点上侧标记已斩。
- 下滑/点下侧标记重难点，不跳词，不暂停，反馈保持轻量。
- 下次进入主学习时，已斩词不再出现在主学习队列。
- 回退到已标记词时出现“撤销上滑”或“撤销下滑”；自动播放再次走到已标记词也要显示对应撤销按钮；撤销按钮不触发暂停。
- 重难点词库模式下，当前 Unit 重难点和整本重难点都能启动；恢复进度不影响主词库恢复位置；重难点为空时有明确提示。
- 阶段总结、继续下一组、本轮重难点复盘可用。
- 返回设置页、切后台、进入总结页会触发静默同步。
- GitHub Pages 或本地 HTTP 刷新后能拿到新版资源。

## 9. 不要随意改动的高风险点

- `triggerCardDirection()` 的左右矩阵。
- `cardActionFromDirection()` 和右侧提示栏 `←/→` 的业务动作映射。
- `state.navQueue` 的连续点击队列；上一词/下一词不能在动画中丢点击。
- `bindCardGesture()` 里暂停态优先恢复播放的顺序。
- `revealZhAfterDelay()` 独立于英文朗读的启动方式。
- `zhRevealDelayMs()`、`preReadDelayMs()`、`postZhRetentionPauseMs()` 都必须保持绝对时间，不要再除以倍速。
- `speakWithHighlight()` 中朗读开始后不强制取消的策略。
- `resumePlayback()` 中的 `state.resumeFeedback`；暂停恢复必须有轻量反馈，但不能加遮罩。
- `markUnknownInPlace()`；下滑只标记重难点，不进入下一词，不做大幅飞出动画。
- `renderWordCard()` 的下一张卡中文不渲染/保持隐藏，避免新卡中文瞬间闪现。
- `loadUnknownProgress()` / `saveUnknownProgress()` 与主词库 `progress:<bookId>` 的隔离关系。
- `advanceWord()` 和 `goPrevious()` 对 `cardEnterDirection` 的设置。
- `commitCurrentCardActivity()` 与 `state.currentWordId` / `state.currentWordRecorded` 的去重关系。
- Gist 空数据、损坏数据和冲突处理逻辑。
- Service Worker 的 Network-First 核心文件策略。
- `.gitignore` 的 `*.md` 规则；Markdown 是内部资料，不应提交到 GitHub。

## 10. 当前正确方向的最短记忆版

后续如果只记一段，请记这一段：

```text
左滑 = 下一个，旧卡向左飞出。
右滑 = 上一个，旧卡向右飞出。
点左侧 = 上一个，旧卡向右飞出。
点右侧 = 下一个，旧卡向左飞出。
右侧提示条 = 可点击控制，不只是说明文字；← 上一个，→ 下一个。
点中间 = 暂停；暂停后再点当前卡片 = 恢复播放。
恢复播放 = 轻量“继续播放”反馈，不加遮罩。
上滑/↑ = 标记已斩并进入下一个。
下滑/↓ = 只标记重难点，不进入下一个，不暂停。
已斩词 = 下次进入主学习时过滤；本次队列内可及时回退撤销。
撤销按钮 = 必须区分“撤销上滑”和“撤销下滑”。
zhDelay 为 0 = 中文立即显示。
朗读倍速 = 只控制中英文读音速度。
读前停留 / 中文延迟 / 读后停留 = 绝对毫秒值，不随朗读倍速缩放。
手动模式 = 当前卡走完整时序后不自动跳下一个。
重难点词库 = 使用 marks.unknown，进度键独立于主词库。
连续点击上一词/下一词 = 必须按点击次数执行，不能被动画吞掉。
朗读开始后不要用硬超时取消，否则会截断读音。
Markdown = 本地内部资料，*.md 已 ignore，不要传 GitHub。
```
