# 2026-09-02 当前版本补充审查

当前发布版本为 `2026-09-02-round-archive-v2`。下方保留的主体报告是 2026-07-25 的历史独立审查基线；本次在其同步安全门禁之上新增轮次归档容量回归检查。

2026-09-02 实机日志确认上一版归档后可因本地备份重复复制完整 `archives` 耗尽 localStorage，随后同步在 `pre_overwrite` 阶段以 `local_backup_write_failed` 失败。本版已改为历史归档本体只存一份，本地安全备份排除重复历史；启动自动压缩旧备份，quota 时只回收冗余备份，不触碰当前学习数据或真实历史归档。新增专项 quota 回归和 rescue 历史保留断言后，全量发布测试通过。

---

# 刷词机最终独立审查报告

版本：`2026-07-25-final-audited-v2`  
审查对象：用户提交的 `final-robust-v1`，并与此前 P14 版本逐文件对比  
审查范围：全部源码模块、生产加载器、生成 bundle、Service Worker、救援页、三份 CSV、测试、文档和发布文件

## 1. 最终结论

提交版的大陆网络改造方向正确：公开 Gist 改为匿名 GET 优先，普通 fetch 网络失败后增加匿名 JSONP 回退，PAT 只在必要的认证读取和写入中使用；相比 P14，重试、锁、生命周期、备份、诊断和离线机制均明显更稳。

独立审查仍发现两个会造成真实数据风险的高影响缺陷：

1. **远端变化 + 本地被标记 clean 时直接 Pull**，在两设备并发覆盖场景中可能让本设备独有记录永久消失；
2. **云端数据逐键写入本地且无完整事务回滚**，中途失败会留下半旧半新的混合状态。

本版已修复上述问题，并补齐网络预检、PATCH 大载荷、pending 标志丢失、设备时钟漂移、启动后错误全屏锁死和过长模块等问题。全部自动测试通过。可作为当前发布候选，但仍必须接受“纯前端无法保证任何中国大陆/VPN链路都能访问 GitHub PATCH”的外部边界。

## 2. 版本对比

| 项目 | P14 | 用户提交 robust-v1 | 本次 audited-v2 |
|---|---:|---:|---:|
| 可维护源码模块 | 37 | 40 | 44 |
| 可维护源码行数 | 9,568 | 9,884 | 9,999 |
| 最大源码模块 | 868 行 | 735 行 | 451 行 |
| 匿名 Gist GET | 无/不足 | 有 | 有，并防止自定义头触发额外预检 |
| JSONP 只读回退 | 无 | 有 | 有，回归测试固定 |
| PATCH 前重读/写后验证 | 不完整 | 有 | 有，并覆盖 clean-local 并发丢失场景 |
| 云端应用事务回滚 | 无 | 无 | 有 |
| pending 游标自重建 | 无 | 无 | 有 |
| 未来时钟漂移保护 | 无 | 无 | 有 |
| 启动后错误非阻断 | 无 | 无 | 有 |

`app.bundle.js` 是自动生成产物，不计入源码行数上限。

## 3. 已确认并修复的问题

### H0-1：clean 本地遇到新远端时直接 Pull，可能丢多设备记录

**原逻辑**  
`decideSyncAction` 在本地 `localDirty=false`、远端相对基线变化时选择 `LOCAL_CLEAN_REMOTE_CHANGED_PULL`，允许直接用远端覆盖本地。

**真实失效序列**

1. 设备 A、B 都读取远端 R0；
2. A 修改记录 A1 并上传 RA；
3. B 仍基于 R0 修改 B1，随后覆盖为 RB；
4. A 本地已被标记 clean，但仍保存 A1；
5. A 看到远端 RB 后直接 Pull，A1 从 A 和远端同时消失。

本地 clean 只证明 A 在上次确认后没有继续改动，不证明当前远端完整包含 A 保存的所有历史记录。

**修复**

- 远端变化且本地存在业务数据时统一进入 `LOCAL_CLEAN_REMOTE_CHANGED_SAFE_MERGE`；
- 删除 clean-local 覆盖式 Pull 特权和 `allowCleanLocalOverwrite`；
- Pull 仅允许“本地无业务数据、远端有效非空”；
- 合并结果等于远端时不产生无意义 PATCH；否则写前再读、重合并并验证。

**回归测试**  
构造本地仅有 A 记录、远端仅有 B 记录且本地 clean 的场景，强制断言 `shouldMerge=true`、`shouldPull=false`。

### H0-2：远端应用不是事务，中途失败会污染本地

**原逻辑**  
远端 payload 依次写进度、标记、活动、统计、pending 和 syncMeta。任一 `localStorage.setItem` 中途失败，只停止后续流程；之前写成功的键不会恢复。

**风险**

- 当前页面继续读取混合状态；
- 业务哈希、dirty 和显示状态可能互相矛盾；
- 即使有覆盖前备份，用户仍需要手动恢复才能回到一致状态。

**修复**

- 写入前捕获全部受影响 localStorage 键及关键内存状态；
- 任一业务键、pending、syncMeta 写入失败，或写后业务哈希不一致，立即恢复原键集合和内存状态；
- 回滚失败时设置 `localRecoveryRequired`，保留覆盖前备份并要求进入救援流程；
- 审计日志记录 rollback 成功/失败和原因。

**回归测试**  
在写 `mark_states:<book>` 时注入 `SecurityError`，断言远端应用返回 false、业务 hash 与进度键完全恢复到事务前。

### S1-1：运行期单个错误会把已启动应用变成全屏红屏

**原逻辑**  
全局 `error`/`unhandledrejection` 不区分启动阶段和运行阶段，任何未处理错误都可能覆盖整个页面。

**修复**

- `99-bootstrap.js` 仅在初始化完成后设置 `window.__SHUA_APP_READY__=true`；
- 启动前错误继续全屏阻断；
- 启动后错误改为底部可关闭警告，保留本地学习和导出能力。

### S1-2：额外 API 版本自定义头会增加浏览器 CORS 兼容风险

审查过程中曾考虑给认证 GET/PATCH 添加 `X-GitHub-Api-Version`，最终从所有浏览器请求中撤销。GitHub 的 CORS 文档示例允许头列表未列出该版本头，而 GitHub 当前明确说明省略版本头时默认使用 `2022-11-28`；保留额外头没有必要，并可能增加预检兼容风险。

**最终规则**

- 匿名 GET：只发送简单 `Accept`，无 PAT、无版本自定义头；
- 匿名 fetch 网络失败：JSONP 只读回退；
- 认证 GET/PATCH：携带必要的 PAT/内容类型，但不额外发送版本头；当前由 GitHub 默认到 `2022-11-28`。

自动测试明确断言匿名请求不存在 `Authorization`，并断言匿名、认证 GET 和 PATCH 均不出现额外版本头。

### S1-3：大 Gist 内容可能截断，写后确认链路变得不可靠

GitHub Gist 元数据对大文件可能不给完整内联内容，而要求访问 `raw_url`。在中国大陆或受限代理网络中，多一条 raw 域名链路会显著增加确认失败概率。

**修复**

- PATCH 前对紧凑 `sync.json` 进行 UTF-8 字节计数；
- 超过 900 KiB 停止上传并记录 `payload_too_large`；
- 不通过无限放宽上限掩盖异常数据膨胀。

### S1-4：持久游标已保存但 pending 标志写失败，重启后可能忘记上传

**修复**

启动时比较持久游标与正式云进度。只要位置不同，即使 pending 标志缺失，也重建 pending、持久化标志并写审计事件。

### S1-5：设备时钟跑到未来，LWW 标记可能永久压制其他设备

**修复**

新本地标记的 `updatedAt` 不再只取当前墙钟，而是推进到本地已观察到的最大标记时间之后。即使已有记录来自 2099 年，后续真实本地操作仍能产生更大的逻辑时间并参与冲突决胜。

### M2-1：核心文件过长，完整审查和回归定位困难

**拆分结果**

- `08-sync-hash-state.js` → `08a-sync-hash-core.js`、`08b-sync-hash-status.js`、`08c-sync-error-state.js`；
- `27-sync-tick.js` 的编排辅助 → `27c-sync-orchestration-helpers.js`；
- `28-sync-push-patch.js` 的分支逻辑 → `28a-sync-branches.js`。

受审核心模块均不超过 500 行；静态测试将此作为发布门禁。拆分未改变存储 schema、同步文件格式或用户操作。

## 4. 大陆网络/VPN专项结论

### 已改善

- 公开 Gist 首次读取不再发送 PAT；
- 匿名 GET 失败可用 `<script>` JSONP 只读回退；
- 匿名请求保持简单，不人为引入预检；
- 认证读取仅在私有/权限场景使用；
- 失败按结构化阶段、传输方式和 HTTP 状态记录；
- 自动重试串行且持久退避，页面隐藏/关闭不发起远端事务；
- PATCH 结果未知时回读确认，不盲目连续覆盖。

### 无法由本项目彻底消除

写入 Gist 必须向 GitHub API 发起带认证 PATCH。若当前网络、VPN节点、浏览器扩展、企业代理或运营商阻断 `api.github.com`、OPTIONS 预检或 PATCH，请求仍会失败。纯静态 GitHub Pages 页面没有后端代理，不能绕过这种外部阻断。

本版的鲁棒性目标是：**云同步可失败，但本地学习、保存、备份、诊断、回滚和网络恢复后的继续同步不能失控。**

## 5. 其他审查结果

### 安全与输入输出

- 未发现仓库中硬编码 GitHub PAT；静态测试覆盖常见 token 格式；
- 排查包不导出明文 PAT；完整本地备份仍可能包含 PAT，文档已明确仅限用户自行保管；
- 动态 HTML 关键路径检查了 `escapeHtml` 使用，未发现明显的可控字段直接拼接漏洞；
- CSP 不允许任意内联脚本，JSONP来源限制为 GitHub API；
- 客户端访问码 `ky2027` 不是安全认证，只能作为界面门槛。

### Service Worker

- 程序壳用 `cache.addAll` 原子安装；资源缺失时不接管残缺版本；
- GitHub API/raw 请求绕过 Service Worker；
- 网络优先请求遇到非成功响应可回退已知缓存；
- 更新期间学习中或本地 dirty 时避免强制刷新。

### 救援与备份

- `rescue.html` 可扫描本地、导出完整备份/脱敏包、恢复 `markStates` 并尝试回滚；
- 主同步覆盖前创建 `pre_overwrite` 备份；
- 云端日备份保留最近 7 天；
- 正常 PATCH 使用紧凑 payload，不重复上传完整旧副本。

### 重复和死代码

- 自定义扫描覆盖 475 个函数声明，未发现重复函数名；
- 已删除/禁止重新引入旧 outgoing pendingOps 生产路径、重复活动模块、空旧兼容文件和唯一确认未使用的 `SYNC_SCHEMA_VERSION` 常量；
- 历史文档保留在 `docs/archive/`，不作为当前设计依据。

## 6. 自动验证结果

发布门禁执行：

```bash
npm run build
npm test
```

覆盖内容：

- 44 个源码模块、loader、bundle、Service Worker、救援脚本语法；
- bundle 与源码模块列表/顺序一致；
- 本地存储失败、配额、安全错误和事务回滚；
- 同步哈希、空云保护、Pull/Push/Merge 决策；
- 两设备标记合并、tombstone、clean-local 并发覆盖回归；
- 持久游标重建和未来时钟逻辑时间；
- 匿名无 PAT/无预检头、JSONP 回退、限流时间；
- PATCH 紧凑传输、日备份、7 日保留、载荷上限和写后确认；
- 审计日志保留、Service Worker 原子缓存、版本一致性、凭据扫描；
- rescue 恢复/回滚和三份 CSV 完整性。

结果：全部自动测试通过。

额外检查：

- 可维护源码模块：44；
- 全局函数声明扫描：475，重复 0；
- 受审核心模块最大 451 行；
- 发布 ZIP 完整性和 SHA-256 清单在打包后验证。

### 浏览器级验证限制

当前容器中的 Chromium 在建立页面请求前即因运行环境启动卡住，因此没有把该次 headless smoke 计为通过。Node/VM 自动测试覆盖了加载顺序、DOM模拟、同步决策和网络回退，但发布后仍应按维护清单完成中国大陆真实手机/平板链路验证。

## 7. 保留的中低影响架构边界

### R1：PAT 存在 localStorage

纯前端需要在浏览器中持有写入凭据。CSP、无第三方脚本和脱敏诊断降低了泄漏概率，但无法抵御恶意浏览器扩展、共享设备或同源脚本供应链失陷。建议使用最小权限、可随时撤销的 PAT，不在公共设备保存。

### R2：活动统计的并发增量可能低估

当前活动秒数/次数和 Unit 计数使用 `max` 合并，保证重复同步幂等，却无法精确相加两设备真正并行产生的独立增量。直接改成求和会产生重复累计。若未来必须精确，需要为事件分配稳定 ID，或改为按设备分量的 G-Counter/事件集合，并执行 schema 迁移。

### R3：客户端访问码不是安全边界

前端源码和访问码对用户可见。若需要真正访问控制，必须引入服务端认证或受控托管，不应继续强化客户端混淆。

## 8. 发布前真机必测

1. 中国大陆宽带/移动网络：不开 VPN、两个不同 VPN 节点分别测试；
2. 手机和平板从同一远端版本分别修改不同标记后连续上传，确认两条记录都保留；
3. 同一单词相反标记，确认后操作胜出；
4. PATCH 发出后立即断网，确认进入“写入待确认”并能恢复；
5. localStorage 被禁用/配额不足，确认远端应用回滚；
6. PAT 失效、Gist ID 错误、403/404/429/5xx；
7. 两标签页、切后台、强杀浏览器、离线重开和 Service Worker 升级；
8. `rescue.html` 备份、脱敏、恢复和回滚。

## 9. 官方技术依据

- GitHub REST API 版本：<https://docs.github.com/en/rest/about-the-rest-api/api-versions>
- Gist REST API：<https://docs.github.com/en/rest/gists/gists#get-a-gist>
- GitHub REST CORS/JSONP：<https://docs.github.com/en/rest/using-the-rest-api/getting-started-with-the-rest-api#using-cross-origin-resource-sharing>


## 2026-07-27-final-ux-sync-v3 增量审查附录

本增量版本未改动上一轮的数据模型、远端格式、合并算法和事务回滚规则。新增的自动同步开关在统一调度器与同步核心双层设门，关闭后只保留本地持久化和显式手动同步；手动同步继续复用原安全链路。顶部启动入口、暂停反馈和归档折叠均为表现层修改，并由发布门禁检查唯一按钮、局部状态更新、无模糊暂停样式和默认折叠结构。


## 2026-07-27-final-ux-sync-v4 增量审查附录

### 已确认并修复

1. **归档看似使用 `<details>`，实际仍全展开**：作者样式 `.word-list { display:grid }` 覆盖了浏览器对未打开 details 子内容的默认隐藏。现新增更高优先级的 `.unit-group:not([open]) > .word-list { display:none!important }`，并在打开归档/切换标签时清空展开集合。
2. **归档选择容易混淆**：Unit 与单词改为互斥选择模式，分别使用 `unit:<id>`、`word:<id>` 抑制长按后的合成 click；移动超过 10 px 取消长按。
3. **批量撤销的同步语义**：不是删除 markStates 记录，而是写入 `value:null` tombstone，保证其他设备能合并到撤销结果。逻辑序号先持久化，标记失败时不会复用序号。
4. **启动路径不连续**：新增本地学习会话记录，认证后不再渲染设置页，恢复完成后再启动同步，避免同步应用与 loading 视图竞态。
5. **手动中文可能与暂停冲突**：开启手动中文后，卡片中部与右侧点击都优先显示中文。暂停态第一次点击只显示中文并保持暂停，第二次点击才恢复播放；播放态点击会停止当前英文阶段并进入中文阶段，不触发切词或暂停。
6. **平板滑动费力**：显示位移采用非线性函数，判定使用放大位移与原始速度；点击抑制继续使用原始距离，避免放大后把轻触误判为拖动。
7. **同步复审**：确认 `initializeSync`、heartbeat、`scheduleSyncSoon`、`requestFreshRemoteCheck`、active-study upload 与 `syncTickInternal` 均保留自动同步关闭门禁；云配置保存也不会在关闭状态上传业务数据。

### 验证边界

- 自动化测试覆盖 46 个源码模块、bundle、同步核心、网络回退、事务应用、数据完整性和新增 UX 状态机。
- 当前最大源码模块 462 行；顶层函数声明重复扫描结果为 0。
- 容器 Chromium 受 URLBlocklist 类组织策略限制，本地 URL 无法加载；未声称完成不可执行的浏览器截图测试。
- 平板触控手感仍需目标设备最终验收，重点检查非线性增益是否符合个人习惯。


## 2026-07-27-final-audio-v5 增量审查附录

### 新增能力

- 英文播放不再直接依赖设备语音。优先使用同源离线音频；缺失时下载标准美式词典录音并写入独立缓存，最后才回退 `speechSynthesis`。
- 设置页提供当前 Unit 预缓存和读音缓存清理；普通学习只预取当前及后续少量词，避免一次性下载全部词库。
- 提供完整音频包构建器，扫描结果固定为 4515 个唯一词，支持断点续传和原子清单写入。

### 审查中发现并修复

1. 初版音频模块重复声明 `objectUrl`，会直接造成语法失败；已删除并由全量 `node --check` 覆盖。
2. 原 CSP 未允许词典 API 与 `blob:` 媒体，远程音频即使下载逻辑正确也无法工作；已增加精确的 `connect-src` 和 `media-src`。
3. 瞬时网络失败原本会把单词加入会话级永久不可用集合；已改为短暂冷却，并在成功后清除。
4. 已缓存的远端 URL 失效后原本会反复尝试；现在失败时删除旧索引，再重新查询。
5. 原播放状态只显示“朗读英文”；现在可区分“标准美音”和“离线标准美音”。
6. 切词与暂停路径原本只取消 Web Speech；现在同时停止 HTML Audio 并释放 Blob URL。

### 保留边界

- 当前 ZIP 不直接附带第三方音频或 Piper 模型。首次遇到未打包单词仍需要网络，之后可离线使用缓存。
- 要实现首次打开即全部离线且声音完全固定，必须运行构建器并部署生成的 `assets/audio/en-us/files/`。
- 外部词典音频的逐文件授权需由实际部署者核对；Piper 模型也必须按其许可证使用。
