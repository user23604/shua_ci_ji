# 刷词机最终独立审查报告

版本：`2026-07-26-final-stable-v3`  
审查基线：`shua_ci_ji-final-audited-v2`  
审查范围：全部 44 个源码模块、生产 bundle、加载器、Service Worker、救援页、三份 CSV、测试、文档和发布文件

## 1. 最终结论

上一版对中国大陆/VPN网络、匿名 Gist 读取、认证 PATCH、退避、确定性合并和本地事务的方向正确。第二次独立复审发现并修复了五类仍可能影响数据安全或恢复闭环的问题：

1. 回滚本身失败后，硬恢复锁可能被“当前数据合法且非空”错误解除；
2. 历史阻塞错误会否决一次已经完整验证成功的 PATCH；
3. 备份优先级分数被毫秒时间戳淹没；
4. 权威本地 JSON 损坏会被静默当作空数据；
5. 活动草稿清 pending 早于 dirty 持久化，存在极小强杀窗口。

上述问题均已修复并新增回归测试。用户确认手机和平板不会同时学习，因此没有为并发活动计数引入事件日志、设备分量或 schema 迁移；顺序切换仍按现有单 Gist/单文件逻辑工作。

## 2. 本轮修复

### P0：硬恢复锁被错误自动解除

**风险**：本地多键事务回滚失败后，数据可能处于部分新、部分旧的混合状态。旧逻辑只要重启后 payload 可解析且非空，就可能解除 `localRecoveryRequired`，随后将混合状态上传。

**修复**：

- `localRecoveryRequired` 增加原因、设置时间、来源和检查点哈希；
- 启动只保留并展示恢复锁，不依据“非空”自动解除；
- 事务回滚后重新采集业务数据并与事务前哈希比较；
- 只有哈希验证成功的回滚/明确备份恢复，或用户在独立救援流程中明确确认，才能解除锁；
- 同步主流程在任何网络请求前检查该锁。

**覆盖文件**：`03-domain-defaults.js`、`08a-sync-hash-core.js`、`08b-sync-hash-status.js`、`09a-sync-backups.js`、`26-sync-apply.js`、`27-sync-tick.js`、`99-bootstrap.js`。

### P1：历史 blocker 否决新鲜 PATCH 成功

**风险**：用户修复 PAT/VPN 后，PATCH 已成功、写后读回 hash 也一致，但旧认证/合并错误仍导致页面保持 dirty，并误报“本地在验证期间发生变化”。

**修复**：写后确认只依据当前上传 hash、当前本地 hash、pending 状态、run 有效性和硬恢复锁。完整的新鲜远端验证是新的权威事实，可清除历史 blocker。

**覆盖文件**：`28-sync-push-patch.js`、`08c-sync-error-state.js`。

### P1：本地权威 JSON 损坏静默为空

**风险**：`progress:*`、`mark_states:*`、`activity:*` 等键解析失败后返回空 fallback，可能导致空数据参与 hash、合并或上传。

**修复**：

- 识别权威业务键；
- 解析失败或根结构异常时保存原始文本到 `vocab_machine_corrupt_storage:*`；
- 记录 `local_storage_corrupt` 和具体键/错误；
- 设置硬恢复锁，阻止云同步；
- 救援页导出包含隔离原文，便于人工恢复。

**覆盖文件**：`02-storage-basic.js`、`08c-sync-error-state.js`、`07-sync-diagnostics-ui.js`、`assets/rescue/rescue.js`、`99-bootstrap.js`、`27-sync-tick.js`。

### P2：备份选择优先级错误

**风险**：将“类型分数”与 `Date.parse()` 毫秒数相加，导致几百点安全权重无效，最新普通备份可能压过事务专用 `pre_overwrite` 检查点。

**修复**：改为词典序排序：有效性 → 恢复原因匹配 → 备份类型 → 时间。普通空本地恢复仍优先最近快照；事务/覆盖失败优先对应覆盖前检查点。

### P2：活动草稿持久化顺序

**风险**：草稿已合并并清 pending、但 dirty 尚未落盘时浏览器被强杀；若变化只有秒数，重启可能不知道尚未上传。

**修复**：正式活动写入成功后，先持久化业务变更/dirty，再清除 activity draft pending。

## 3. 中国大陆/VPN路径

当前路径保持：

```text
公开 Gist：匿名 GET
  → 网络/超时失败：匿名 JSONP
  → 私有/权限场景：认证 GET
写入：认证 PATCH
```

匿名读取不带 PAT、不发送额外 `X-GitHub-Api-Version`。认证写入必须由浏览器向 GitHub 发出 OPTIONS/PATCH；只要 VPN实际代理相关域名和方法，代码不会再被旧版“认证 GET 先失败、匿名回退永远进不去”的逻辑阻塞。

程序对网络失败执行串行、持久退避；页面隐藏/关闭不发起远端事务；PATCH响应丢失先回读确认。无法从静态容器伪造真实大陆网络，因此“真机两个 VPN 节点成功”未写成已完成事实。

## 4. 多设备边界

标记、进度和远端变化继续确定性合并，clean 本地不能盲 Pull。活动秒数、次数和 Unit 完成数仍以 `max` 保持重复同步幂等；真正同时学习可能低估独立增量。用户已确认设备按顺序使用，因此本版选择不引入高复杂度 CRDT/事件迁移。实际切换前应等待“云端已保存”。

## 5. 维护性

- 44 个可维护源码模块，生产 bundle 自动生成；
- 可维护源码 10,243 行，最大核心模块 `26-sync-apply.js` 为 462 行；
- 509 个顶层函数声明，重复 0；
- 无确认的 TODO/FIXME/HACK/XXX；
- 无仓库硬编码 GitHub token 模式；
- 版本、模块顺序、Service Worker 程序壳、救援资源均由自动测试固定。

没有为“看起来更简洁”重写稳定学习功能，也没有改变 Gist schema。拆分只用于已有独立职责；生成 bundle 不参与源码行数限制。

## 6. 自动测试

`npm run build && npm test` 全部通过，覆盖：

- loader、bundle顺序和全部 JS 语法；
- 本地存储失败、配额、远端事务回滚和回滚失败；
- 恢复锁重启保留及哈希验证解除；
- 权威 JSON 损坏隔离；
- clean-local 远端变化合并；
- 历史 blocker 后新鲜 PATCH 成功；
- 备份优先级；
- 活动草稿 dirty/pending 顺序；
- 匿名 GET、JSONP、认证回退、限流和 PATCH确认；
- 持久游标、逻辑时间、紧凑传输、900 KiB保护；
- Service Worker、救援恢复、PAT扫描和三份 CSV完整性。

Headless Chromium 在当前容器中仍在发出本地页面请求前超时，因此没有伪报浏览器级 smoke 通过。Node/VM 和静态 HTTP资源检查不替代部署后的 Android/Edge/Chrome 真机测试。

## 7. 发布后真机验收

1. 同一手机不开 VPN、开启两个不同 VPN 节点，分别执行“测试并保存”和真实同步；
2. 手机学习并显示云端已保存后，在平板打开并继续；反向再测一次；
3. PATCH发出时断网，确认进入写入待确认并能回读恢复；
4. PAT失效、Gist ID错误、403/404/429/5xx；
5. 切后台、强杀重开、双标签、离线启动和 Service Worker升级；
6. 注入 localStorage 写入/解析错误，确认恢复锁不自动解除并可由 `rescue.html`恢复。

## 8. 最终外部边界

纯前端不能强制不可达的 GitHub链路变得可达，也无法保护保存在浏览器中的 PAT免受恶意扩展/同源供应链攻击。当前版本的保证是：网络和认证可以失败，但本地学习、数据安全、回滚、诊断、退避和恢复流程不能因此失控或假报成功。
