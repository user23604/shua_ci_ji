# P14.3 交付说明：polish-and-deep-test

版本：`2026-06-21-p14-3-polish-and-deep-test`

本版基于 P14.2，集中处理上一轮 v5 报告中剩余的体验/测试严谨性尾巴。

## 本版修复

1. 配置按钮状态明确化
   - 新增 `setCloudConfigStatus(message, kind)`。
   - `#cloudConfigStatus` 增加 `data-sync-config-status`。
   - 点击“测试并保存云同步配置”后立即进入 `testing` 状态。
   - GET Gist 成功后显示“已连通 Gist，正在测试写权限…”。
   - 成功/失败均显式写入 `ok/error` 状态。
   - 同步写入 `window.__lastCloudConfigStatus` 并派发 `cloud-config-status-change` 事件，便于自动测试和人工诊断。

2. 减少 clean 状态 active-study 空转
   - 新增 `activeStudySyncWorkExists()`。
   - 如果没有 `localDirty`、没有 hash dirty、没有 pending progress/activity、没有 verify 派生 dirty，则不再安排 `active_study_idle_upload`。
   - clean 状态下自动清理 pending timer，避免“已干净但继续 no-op 检查”。
   - `syncTick.finally` 不再因为旧的 `pendingActiveStudyUpload=true` 而重新安排无意义同步。

3. 继续保留 P14.2 已修复项
   - `BUSINESS_HASH_SCHEMA_VERSION=3`。
   - hidden 状态不主动 active-study PATCH。
   - hidden/background `Failed to fetch` 降级，不误红。
   - stale dirty 自愈。
   - 可恢复 sync-problem-dialog 自动关闭。
   - 启动同步锁/Service Worker reload 期间点击开始刷词的竞态缓解。

## 静态检查

已执行：

```bash
node --check app.js
node --check sw.js
for f in assets/js/*.js assets/rescue/*.js tests/*.js; do node --check "$f" || exit 1; done
python3 -m json.tool version.json >/dev/null
node tests/p14_sync_core_test.js
node tests/p14_loader_smoke_test.js
```

全部通过。
