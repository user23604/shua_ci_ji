# 更新记录

## 2026-07-27-final-ux-sync-v4

- 登录后与已登录启动时直接恢复上次未完成的刷词位置；进入卡片后固定为暂停状态，不自动播放。
- 新增本地学习会话游标，仅保存在当前浏览器，不进入云同步业务包。
- 归档 Unit 使用显式 `details:not([open])` 隐藏规则，默认全部折叠，修复作者样式覆盖浏览器原生折叠的问题。
- 长按 Unit 进入 Unit 多选，顶部显示“开始刷词”，可组合刷所选 Unit 在当前归档标签中的单词。
- 展开 Unit 后长按单词进入独立的单词多选，顶部显示“撤销”，批量清除所选单词的已删/重难点标记。
- Unit 选择和单词选择使用互斥状态与独立长按键，避免手势混淆；滚动超过阈值会取消长按。
- Unit 完成页新增“本单元从头再刷一遍”，包含本 Unit 全部单词，不过滤已标记词。
- 设置页新增“手动显示中文”；开启后延迟值失效，中文保持隐藏，点击卡片中部或右侧后才显示。
- 平板卡片滑动改为非线性增益：小幅滑动更灵敏，大幅滑动按更高增益移动，同时降低误触阈值。
- 批量撤销先持久化逻辑序号再写标记；失败只留下安全序号空洞，不复用序号。
- 复审自动同步关闭门禁：启动、心跳、联网恢复、归档检查和学习空闲上传仍全部被拦截；仅显式手动同步放行。
- 新增 UX v4 回归测试，覆盖折叠、长按选择、整 Unit 重刷、手动中文、非线性滑动、直接恢复及同步关闭门禁。

## 2026-07-27-final-ux-sync-v3

- 将唯一“开始刷词”入口移动到设置页顶部并设为 sticky；加载状态改为局部更新，修复点击后长页面回顶、需要再次滚动的问题。
- 新增本机 `autoSyncEnabled` 开关，旧用户迁移默认开启。
- 自动同步关闭时，中央 `syncTick`、统一调度器、学习空闲上传、heartbeat、启动初始化、前后台切换、网络恢复和归档/统计远端检查全部停止；本地写入、快照和备份继续工作。
- 自动同步关闭时显示显式手动同步按钮；手动同步继续走安全合并、预读、写后校验和回滚链路。
- 云配置测试在自动同步关闭时只验证配置和写权限，不自动上传业务数据。
- 增加持续暂停标识和轻量边框反馈，不模糊卡片内容；恢复时保留原“继续播放”短反馈。
- 归档 Unit 默认折叠。
- 增加对应静态与状态机回归测试。

# Changelog

## 2026-07-25-final-audited-v2

### 高影响修复

- 远端发生变化时，即使本地标记为 clean，只要本地仍有业务数据也必须确定性合并；删除 clean-local 直接 Pull 覆盖路径。
- 云端数据应用本地改为完整事务；业务键、pending、syncMeta 或写后哈希任一失败都会恢复操作前状态。
- 启动成功后的运行时错误改为可关闭警告，不再用全屏红屏锁死已可用应用。

### 网络与传输

- 浏览器端 GET/PATCH 不发送 `X-GitHub-Api-Version`；GitHub 当前在省略该头时默认使用 `2022-11-28`，同时减少 CORS 预检请求头。
- 所有 Gist 浏览器请求均不发送 `X-GitHub-Api-Version`；匿名 GET 还明确不发送 PAT，避免增加 CORS 预检失败点。
- 紧凑 `sync.json` 超过 900 KiB 时停止 PATCH并给出结构化诊断，避免大 Gist 截断/raw 二次读取削弱写后确认。

### 本地持久化与多设备

- 启动时比较持久游标与正式进度；pending 标志丢失时自动重建待同步状态。
- 新标记时间推进到已观察最大时间之后，避免设备时钟异常超前导致其记录永久获胜。
- 只读合并和可写合并在结果已等于远端时不产生无意义 PATCH。

### 维护性与验证

- 拆分哈希/状态/错误、同步编排辅助和 PATCH 分支为 `08a/08b/08c`、`27c`、`28a`。
- 新增并发覆盖、事务回滚、pending 重建、逻辑时间、匿名无预检头和核心文件行数回归测试。
- 受审核心源码模块均不超过 500 行；生成 bundle 除外；删除唯一确认未使用的旧 schema 常量。

## 2026-07-25-final-robust-v1

### 同步可靠性

- Gist 读取改为匿名 GET 优先，网络失败时使用匿名 JSONP 回退，认证读取仅作为最后手段。
- 自动同步请求合并串行执行，取消自动路径绕过退避和页面隐藏/关闭时的远端同步。
- 失败重试改为持久化指数退避，避免请求风暴、跨标签锁堆积和 watchdog 连锁故障。
- PATCH 网络异常增加回读确认；无法确认时进入“写入待确认”，保留本地数据。
- PATCH 前重新读取远端并在变化时重合并，写后验证业务哈希。
- 区分认证失败、限流、404、422、5xx、网络失败、超时和验证失败。
- 读取 GitHub 限流响应头，并按服务器要求延后重试。
- 多标签页优先使用 Web Locks，旧浏览器回退到本地租约锁。
- 正常 PATCH 改用紧凑 JSON；云端每日备份只写一次并自动保留最近 7 天。

### 本地数据安全

- localStorage 配额不足时先清理低价值日志/旧备份，再重试关键写入。
- 所有关键业务保存函数返回结果；失败时不再清除 pending 或误报已保存。
- 应用云端数据前强制写覆盖前备份；备份失败则停止覆盖。
- 应用远端数据后逐项检查保存结果并进行业务哈希校验。
- `markStates` tombstone 在精简、备份和救援流程中保持为业务数据。
- `rescue.html` 恢复改为批量写入、失败尝试回滚，并强制主程序重新计算业务 hash。

### 用户体验和诊断

- 设置页增加明显的同步状态卡、最后成功时间、下次重试时间、立即同步和一键排查包。
- 右上角状态指示器保留颜色并增加明确文字、可点击诊断和无障碍状态。
- 排查包包含结构化错误阶段、传输方式、HTTP 状态、同步状态、业务摘要、备份索引和审计事件，不包含明文 PAT。
- PAT 输入默认隐藏。

### 离线与更新

- Service Worker 提供版本化程序壳缓存；GitHub API/raw 请求始终绕过缓存。
- 新 Service Worker 激活时，学习中或本地 dirty 不再强制刷新页面。
- Service Worker 新版本程序壳原子安装；临时 4xx/5xx 时优先使用已知可用缓存；救援页资源纳入离线壳。

### 维护性

- 将过长的本地存储和同步辅助文件按职责拆分。
- 删除冻结的 outgoing pendingOps、废弃包装函数、空兼容文件和重复活动文件。
- V2 代码改为明确的只读旧数据兼容模块。
- 增加模块图、同步设计、排查手册、发布检查表和自动测试。
- 生产环境使用一个自动生成 bundle，源码模块保留作维护和加载失败回退，减少请求数量但不增加框架依赖。
- 审计日志按价值分级保留，状态渲染和跳过事件不再淹没真正的失败、决策和用户操作。
